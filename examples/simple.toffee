migo = require '..'

{add, migrate} = migo.init(dbname:'migo_test', sslmode:false)
add 'create table foo',
    up: """
            CREATE TABLE foo(
                id Serial,
                name Text
            )
        """
    down: (autocb) ->
        @drop! 'foo'

add 'add index',
    up: 'CREATE INDEX ON foo(name)'

migrate! process.argv[2]
